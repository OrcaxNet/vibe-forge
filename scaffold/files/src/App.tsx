// This is the ONLY writable business file in the Vibe Forge scaffold.
// The Engineer stage overwrites /src/App.tsx; all other scaffold files are
// read-only. Default content below compiles so an empty/failed run still has a
// valid preview surface.
export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50 text-slate-900">
      <div className="text-center">
        <h1 className="text-2xl font-bold">Hello, Vibe Forge</h1>
        <p className="text-slate-500 mt-2">
          The Engineer stage will replace this with your app.
        </p>
      </div>
    </main>
  );
}
